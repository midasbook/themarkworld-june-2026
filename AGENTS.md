# The Mark World Project Rules

이 작업공간에서 더마크월드 랜딩페이지나 앱을 새로 만들 때는 아래 원칙을 따른다.

- 기존 랜딩페이지나 앱의 `index.html`, `styles.css`, 스크립트, 에셋을 덮어쓰지 않는다.
- 매번 새 요청은 별도 폴더에 독립 프로젝트로 만든다.
- 폴더명은 목적과 날짜를 알 수 있게 짓는다.
  - 예: `2026-07-shareholder-strategy-poll`
  - 예: `2026-08-program-landing`
- 각 프로젝트는 자체 `index.html`, CSS, JS, `assets` 폴더를 가진다.
- 배포할 때도 기존 사이트와 섞이지 않도록 별도 사이트 또는 별도 하위 경로로 배포한다.
  - 예: `https://.../2026-07-shareholder-strategy-poll/`
- 관리자 페이지, 응답 저장소, 로컬스토리지 키, API 엔드포인트도 프로젝트별로 고유하게 만든다.
- 기존 프로젝트 파일을 수정해야 할 때는 사용자가 명시적으로 요청한 경우에만 수정한다.
